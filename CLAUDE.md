# CLAUDE.md

Betriebsanleitung für Claude Code in diesem Repo. Kurz halten, strikt befolgen.

## Projekt

Deskwire (Arbeitstitel): SaaS-Web-App, das KI-Interface für Redaktionen. Von Signal zu Idee zu Briefing zu Artikel zu Upload, mit Brand-Profilen, fail-closed QA und konfigurierbaren Upload-Zielen.

**Die inhaltliche Wahrheit steht in `docs/PRD.md`. Vor jedem neuen Feature das betreffende PRD-Kapitel lesen. Bei Unklarheit oder Widerspruch: fragen, nicht raten. Nichts bauen, was im PRD unter Nicht-Ziele steht.**

Aktuelle Phase: **Phase 0 (Foundation) ist abgeschlossen** (Abnahme bestanden 27.07.2026, Tag v0.1.0-phase0). Phase 1 ist noch nicht begonnen; es wird weiterhin nichts aus späteren Phasen vorgezogen.

## Stack (entschieden, nicht diskutieren)

- Next.js (App Router) auf Vercel, TypeScript strict
- Tailwind CSS plus shadcn/ui
- Postgres (Neon) plus Drizzle ORM (Migrations via drizzle-kit)
- Auth.js für Authentifizierung
- Inngest für Hintergrund-Jobs, Agent-Runs und Cron
- Stripe für Billing
- Anthropic API als primärer Modell-Provider, immer über den eigenen Provider-Layer (`src/lib/ai/`)
- Zod für alle Eingaben an Systemgrenzen
- Resend für Transaktionsmails (Magic-Link-Login, Einladungen)
- Sentry für Error-Tracking, ab Phase 0 verdrahtet
- PostHog für Produkt-Analytics, Events ab Phase 1
- Upstash Ratelimit für Rate Limiting öffentlicher und run-auslösender Endpoints
- Vitest (Unit), Playwright (E2E), npm als Paketmanager

Neue Dependencies nur nach Rückfrage mit Einzeiler-Begründung.

## Entschiedene Grundsatzfragen

- Auth: Magic Link via Resend zuerst, OAuth später.
- Recherche im MVP: Websuche des Modell-Providers, ausschließlich über den Provider-Layer. Die Quellenliste jedes Runs wird gespeichert. Eine eigene Fetch-Schicht mit Domain-Whitelist ist eine Phase-2-Option.
- UI-Sprache: Deutsch, i18n-fähig gebaut.
- Produktname: vertagt bis zum stehenden MVP, Arbeitstitel bleibt Deskwire.

## Repo-Struktur (Soll)

```
docs/PRD.md
src/app/            Routen (App Router), Route Handlers unter app/api
src/components/     UI-Komponenten (shadcn-basiert)
src/db/             schema.ts, Migrations, gescopte Query-Helper
src/lib/ai/         Provider-Abstraktion, Prompts, Pipeline-Schritte
src/lib/qa/         deterministische Checks, Platzhalter-Logik
src/lib/billing/    Credits, Gates, Stripe-Sync
src/inngest/        Functions (Runs, Jobs, Ingest, Deliveries)
src/lib/security/   Credential-Verschlüsselung, HMAC-Signaturen
tests/              Unit- und E2E-Tests
```

## Grundprinzipien (nicht verhandelbar)

1. **Das LLM urteilt, der Code entscheidet.** Statusübergänge, Gates, Zahlen, Limits, Platzhalter-Erkennung sind deterministischer Code. Das Modell liefert Sprache und Einschätzungen, nie Kontrollfluss-Entscheidungen.
2. **Fail-closed.** Fehlende oder unsichere Daten erzeugen kanonische Platzhalter (`[einsetzen: ...]`, `[zu klären: ...]`) oder den Status FEHLER. Niemals stillschweigend Ersatz generieren. Ein Artikel mit offenem Marker kann nicht BEREIT werden.
3. **Tenant-Isolation ist heilig.** Jede fachliche Tabelle trägt `workspace_id`. Datenzugriff ausschließlich über die gescopten Query-Helper aus `src/db/`, nie roh am Scope vorbei. Jedes neue fachliche Feature bekommt einen Isolationstest.
4. **Jeder Run ist nachvollziehbar.** Pipeline-Schritte persistieren Input-Referenz, Output, Dauer, Token und Kosten in `run_steps`. Keine LLM-Aufrufe außerhalb des Provider-Layers.
5. **Credentials nie im Klartext.** Quellen- und Ziel-Zugänge werden verschlüsselt gespeichert, nie an den Client geliefert, in Logs maskiert. Secrets nur über Env-Variablen, niemals im Code oder in Fixtures.
6. **Kosten werden gemessen.** Kein LLM-Aufruf ohne Token- und Kosten-Erfassung. Budget-Gates sind Code und laufen vor dem Aufruf.

## Konventionen

- TypeScript strict, kein `any`, kein `as unknown as`. Lieber Typ sauber herleiten.
- Zod-Validierung an jeder Grenze: Route Handler, Server Actions, Inngest-Event-Payloads, Webhook-Eingänge, LLM-Antworten (structured output parsen, nie blind vertrauen).
- Server-first: Datenzugriff in Server Components, Route Handlers und Inngest Functions. Client Components nur für Interaktivität.
- Fehlerbehandlung: erwartbare Fehler als typisierte Ergebnisse zurückgeben, unerwartete werfen und zentral loggen. Nutzer sehen immer eine verständliche deutsche Fehlermeldung mit Handlungsoption.
- Datenbank: Schemaänderungen nur über Drizzle-Migrations. Bestehende Migrationsdateien niemals editieren oder löschen. Vor destruktiven Migrationen stoppen und nachfragen.
- Lokale Entwicklung läuft gegen einen eigenen Neon-Dev-Branch, nie gegen die Produktions-DB. Der Wechsel der lokalen DATABASE_URL auf Production ist ein bewusster Akt des Betreibers und ausschließlich für die Abnahme-Schritte 5 und 7 aus docs/phase-0-plan.md vorgesehen.
- Prompts sind Code: Sie liegen als Dateien unter `src/lib/ai/prompts/`, Änderungen laufen durch Git. Jeder Run speichert die Referenz der verwendeten Prompts (Dateipfad plus Inhalts-Hash). Kein Inline-Prompt-Text in Pipeline-Code.
- UI-Arbeit liest zuerst docs/brand-book.md. Bei Konflikten gewinnt das Brand Book vor dem frontend-design-Skill, der Skill vor allgemeinen Vorlieben. Keine Farben, Schriften oder Motive außerhalb der Brand-Book-Tokens.
- Naming: Code, Identifier, Commits und Code-Kommentare auf Englisch. UI-Texte und Nutzer-Fehlermeldungen auf Deutsch.
- UI-Texte: echte deutsche Umlaute, keine Gedankenstriche (stattdessen Komma oder Punkt), Du-Ansprache, kurz und konkret.
- Kein Datei-übergreifendes Copy-Paste von Logik: Wiederverwendbares gehört nach `src/lib/`.
- Umgebung: Kein Homebrew, niemals. Tools werden ausschließlich über offizielle Installer, direkte Binary-Downloads (GitHub Releases), npm/npx oder Docker installiert. Schlage keine brew-Kommandos vor.

## Arbeitsweise

- **Plan vor Code:** Bei jedem neuen Feature zuerst einen kurzen Plan (betroffene Dateien, Schema-Änderungen, Testansatz) zeigen und Bestätigung abwarten. Bugfixes und Kleinigkeiten direkt.
- Kleine, in sich lauffähige Schritte. Nach jedem Schritt: `npm run lint && npm run typecheck && npm test` grün, sonst nicht weitermachen.
- Tests sind Pflicht für: QA-Gates und Platzhalter-Logik, Status-Übergänge, Billing und Credit-Gates, Tenant-Isolation, Webhook-Signaturen. Zusätzlich laufen die Golden-Briefings (feste Test-Briefings mit automatisierter Prüfung auf Pflichtelemente, Platzhalter-Syntax und verbotene Muster) als Regressionstest nach jeder Pipeline- oder Prompt-Änderung. UI-Feinschliff braucht keine Tests.
- Commits klein und beschreibend (Conventional Commits: feat, fix, chore, test, docs).

**Ohne Rückfrage niemals:**
- neue Dependencies einführen
- Auth-, Billing- oder Security-Code umbauen
- Migrationen löschen oder destruktive Migrationen ausführen
- Daten an neue externe Endpoints senden
- Feature-Umfang über das aktuelle PRD-Kapitel hinaus erweitern
- schreibend auf die Produktions-DB zugreifen, auch nicht durch lokale Test- oder Browser-Läufe, auch nicht mit anschließendem Aufräumen

## Definition of Done (pro Task)

1. Verhalten entspricht dem PRD-Kapitel und den Akzeptanzkriterien.
2. Lint, Typecheck und Tests grün, neue Kernlogik ist getestet.
3. Tenant-Scope geprüft (kein Query ohne workspace_id-Scope).
4. Fehlerfälle behandelt, keine stillen Ausfälle.
5. Keine Secrets, keine TODO-Platzhalter im gemergten Code.
6. Kurzer Eintrag unter "Stand" in dieser Datei aktualisiert.

## Kommandos

Real existierende Scripts (package.json):

```
npm run dev            # lokale Entwicklung
npm run build          # Production-Build (lokal selten nötig, Vercel baut)
npm run start          # Production-Server lokal
npm run lint           # ESLint
npm run typecheck      # tsc --noEmit
npm test               # Vitest
npm run db:generate    # Drizzle-Migration aus Schema erzeugen
npm run db:migrate     # Migrationen anwenden
npm run db:studio      # Drizzle Studio
```

Noch nicht existent, kommt mit der jeweiligen Phase (nicht vorziehen):

```
npm run inngest:dev    # ab Phase 1: Inngest Dev Server (Agent-Pipeline)
npm run test:e2e       # ab Phase 3: Playwright E2E
```

## Stand

- 24.07.2026: Projektstart. PRD v1 liegt in docs/PRD.md. Nächster Schritt: Phase 0 Scaffold (Repo, CI, Auth, Workspaces, Tenancy-Tests, Stripe-Testmode).
- 25.07.2026: Task 1a fertig: Next.js-16-Scaffold (App Router, TS strict, Tailwind v4, ESLint 9 Flat Config), shadcn/ui (Preset radix-nova, Base Color neutral; button, input, card, label), Brand-Token-Fundament aus docs/brand-book.md Kap. 5 (Farb-Tokens, Typoskala, Radii in globals.css; Fonts Besley, Public Sans, IBM Plex Mono self-hosted via next/font; src/lib/brand.ts als Namens-Token). Node-Basis dokumentiert (.nvmrc 24, packageManager npm@11.16.0). Nächster Task: 1b (Vitest plus GitHub-Actions-CI).
- 25.07.2026: Task 1b fertig: Vitest 4 (environment node, tsconfig-Paths nativ via `resolve.tsconfigPaths`, keine Plugin-Dependency), tests/smoke.test.ts, Script `test` = `vitest run`, GitHub-Actions-CI (.github/workflows/ci.yml: Node aus .nvmrc, npm ci, lint/typecheck/test, null Secrets). npm-audit-Notiz: alle Findings liegen in der Toolchain (postcss/sharp gebundelt in Next 16, minimatch-Kette in eslint-config-next), zur Laufzeit nicht erreichbar; kein `audit fix --force` (würde Next auf 9.x downgraden). Vorgehen: bei jedem Task-Start auf Patch-Releases von next/eslint-config-next prüfen. Nächster Task: 2 (Vercel-Deployment).
- 25.07.2026: Task 2 fertig: Vercel-Projekt via GitHub-Import (Node 24.x, Turbopack), main deployt nach Production (deskwire.vercel.app), jede PR bekommt ein Preview-Deployment (durch Vercel-Login geschützt, gewollt). Health-Route `/api/health` (ok plus Commit-SHA, force-dynamic; DB-Ping folgt in Task 4), `.env.example`-Kopf mit Env-Konvention (Wahrheit pro Environment im Vercel-Dashboard, lokal `npx vercel env pull` nach `.env.local`), README-Abschnitt „Entwicklung". Branch Protection auf main: Required Checks `Vercel` plus `ci` (classic, ohne Admin-Enforcement, Direkt-Push auf main bleibt möglich). Beweis erbracht (Test-PR #1, unmerged geschlossen): Build-only-Fehler → CI grün, Vercel rot, `mergeable_state: blocked`. Befund: Turbopack 16.2.11 bricht bei „two parallel pages resolve to /" den Build NICHT mehr, der Konflikt wird still aufgelöst — Task-10b-Annahme anpassen (Root-Page-Löschung bleibt nötig, es warnt nur kein Build-Fehler mehr); zuverlässiger Build-Brecher für Check-Tests: Throw beim statischen Prerender. Nächster Task: 3 (Sentry).
- 25.07.2026: Task 3 fertig: Sentry manuell ohne Wizard (@sentry/nextjs 10.68): src/instrumentation.ts (register je NEXT_RUNTIME plus onRequestError), src/instrumentation-client.ts, sentry.server/edge.config.ts (nur Error-Tracking, kein Tracing/Replay/Logs, environment aus VERCEL_ENV), next.config.ts via withSentryConfig (org maier-ai, project deskwire, sentryUrl de.sentry.io, tunnelRoute /monitoring, widenClientFileUpload, Source Maps nach Upload gelöscht). Debug-Route /api/debug-sentry hinter DEBUG_SENTRY_ENABLED=1 (sonst 404, Gate unit-getestet). Build ohne SENTRY_AUTH_TOKEN grün, Upload wird nur übersprungen (CI-Fall). Beweis: Event DESKWIRE-1 von der Preview-URL, Stacktrace per Source Maps aufgelöst (src/app/api/debug-sentry/route.ts:11 mit Code-Kontext), Turbopack-Build. Befund: sentry-cli bevorzugt die im Auth-Token eingebettete API-URL vor sentryUrl (Build-WARN, harmlos, Upload läuft). DEBUG_SENTRY_ENABLED nach jedem Test wieder aus Vercel entfernen. Nächster Task: 4 (Neon + Drizzle).
- 25.07.2026: Task 4 fertig: Neon + Drizzle (drizzle-orm 0.45.2, drizzle-kit 0.31.10, @neondatabase/serverless 1.1.0, ws, zod, dotenv — exakt gepinnt): ein Treiber neon-serverless (Pool + ws) als lazy Singleton `getDb()` in src/db/index.ts; src/lib/env.ts (Zod, lazy geparst, unit-getestet — typecheck/CI/Build laufen nachweislich ohne Env); drizzle.config.ts lädt via dotenv `.env.local` vor `.env` und migriert ausschließlich über DATABASE_URL_UNPOOLED; Migration 0000 (workspaces + brand_profiles: workspace_id-FK on delete cascade + Index) auf Production angewendet; Health-Route mit select-1-Ping (DB-Fehler → 503 + Sentry-Event, keine Details im Response); Scripts db:generate/db:migrate/db:studio. Env-Verifikation via Vercel: Integration setzt exakt DATABASE_URL + DATABASE_URL_UNPOOLED (nur Production statisch; Preview branch-spezifisch beim Deployment injiziert), keine Zusatzvariablen (kein POSTGRES_URL/PGHOST). Beweis eigener Preview-DB-Branch (PR #3): Preview-Endpoint ep-soft-shape-agxlct2a ≠ Production ep-holy-hat-ag6daxih, Tabellen auf dem Preview-Branch via Fork vorhanden (dort nie migriert), Preview-/api/health db ok. Befunde: `vercel link` hängt ein redundantes `.env*` an .gitignore an (hebelt als letzte Regel `!.env.example` aus) — verworfen, bei CLI-Läufen drauf achten; NEON_BRANCH aus dem Neon-Onboarding ist in .env.example als unbenutzt dokumentiert. Nächster Task: 5 (PGlite-Test-Harness).
- 26.07.2026: Task 5 fertig: PGlite-Test-Harness (@electric-sql/pglite 0.5.4 exakt gepinnt, Dev-Dep; von drizzle-orm 0.45.2 als Peer >=0.2.0 abgedeckt, kein weiteres Paket): tests/helpers/db.ts mit `createTestDb()` — pro Aufruf eine frische In-Memory-Instanz (`new PGlite()` + drizzle-orm/pglite), migriert mit den echten Migrationsdateien aus src/db/migrations via drizzle-orm/pglite/migrator in Journal-Reihenfolge (bewusst kein pushSchema, die Migrationen stehen mit unter Test; Pfad von der Helper-Datei aus aufgelöst, cwd-unabhängig). Muster: eine Instanz pro Testdatei (PGlite ist single-connection), `close()` in afterAll; Isolation strukturell, da nie etwas geteilt wird. tests/db/schema.test.ts beweist: gen_random_uuid() läuft nativ in PGlite (Schema und Migration unverändert), Insert/Select-Roundtrip auf workspaces + brand_profiles, FK-Enforcement, ON-DELETE-cascade, Instanz-Isolation. Beweis offline + secret-frei: `npm test` grün unter `sandbox-exec` mit `(deny network*)` (Netz-Probe scheitert nachweislich im selben Lauf) UND mit `env -i` (nur PATH/HOME, kein DATABASE_URL); CI unverändert ohne Secrets. Laufzeit-Baseline lokal (Referenz für Degradierung): `npm test` gesamt ~1,5 s Wall (Vitest-Duration ~1,2 s, 12 Tests / 4 Dateien); `createTestDb()` cold 567 ms (WASM-Init + Migration), warm 370–400 ms. Typkompatibilität: `TestDb = PgliteDatabase<typeof schema>` ist strukturell kompatibel zum App-Typ `Db = NeonDatabase<typeof schema>` (beide erweitern PgDatabase über demselben Schema) — die Scoped-Helper aus Task 6 MÜSSEN gegen das generische `PgDatabase<..., typeof schema>` typisiert werden, nie gegen einen konkreten Treibertyp, sonst Typkonflikt zwischen App- und Testinstanz. Befund: next/eslint-config-next 16.2.12 erschienen (installiert: 16.2.11), Patch-Upgrade beim nächsten Task-Start einplanen. Nächster Task: 6 (Gescopte Query-Helper).
- 26.07.2026: Task 6 fertig (vorab: next/eslint-config-next-Patch auf 16.2.12, lint/typecheck/test grün): Gescopte Query-Helper als verbindliches Muster. src/db/scoped.ts: `scopedDb(db, workspaceId)` mit brandProfiles.list/getById/create — jede Query hart per eq(workspace_id), getById mit and(eq(id), eq(workspaceId)) → null bei fremd oder unbekannt, create nimmt id/workspaceId per Omit-Typ gar nicht an (Scope liefert die workspaceId). Typisiert gegen generisches `PgDatabase<PgQueryResultHKT, typeof schema>` (Export `DbClient`); Typ-Beweis: derselbe Helper läuft im PGlite-Test (tests/db/brand-profiles.test.ts: zwei Workspaces, list nur eigene, getById fremd → null, create stempelt Scope-Id). src/db/index.ts: `getScopedDb(workspaceId)` als einziger App-Einstieg plus `pingDb()` (select 1) — die Health-Route importiert kein getDb mehr; die neue Lint-Regel fand diesen Verstoß sofort (erster Praxisbeweis). ESLint-Block (files src/**, ignores src/db/**; tests/** liegt außerhalb und ist damit ausgenommen) verbietet: getDb aus @/db und @/db/index (paths), getDb via Relativpfad-Regex `^\.\.?(/.*)?/db(/index)?$` (patterns), und die rohen Treiber drizzle-orm/neon-serverless, drizzle-orm/pglite, @neondatabase/serverless inkl. Subpfade (patterns/group) — sonst baut man sich am Scope vorbei einen eigenen Client. Alle drei Bypass-Wege (Alias, Relativpfad, Treiber-Direktimport) per Temp-Datei-Negativprobe belegt, 4/4 Importe rot. README-Abschnitt „Datenzugriff" ergänzt. Auth-Adapter-Ausnahme folgt erst mit Task 7a als ignores-Eintrag. Nächster Task: 7a (Auth.js-Kern: Adapter, Schema, Magic Link).
- 26.07.2026: Task 7a fertig (PR-Flow): Auth.js-Kern mit next-auth 5.0.0-beta.32 plus @auth/drizzle-adapter 1.11.3 (beide exakt gepinnt, beide auf @auth/core 0.41.3 — vor Upgrade beider Pakete Peer-Gleichstand prüfen). Schema plus Migration 0001: users/accounts/sessions/verification_tokens nach Adapter-Minimalsatz (snake_case-Spalten, TS-Property-Namen exakt wie vom Adapter erwartet, uuid-User-Id via gen_random_uuid, FK-Cascade auf users, Indizes auf user_id), rein additiv, auf Production angewendet (Journal-Eintrag 2 verifiziert). src/auth.ts als Lazy-Config `NextAuth(() => config)` — Modul-Import bleibt env-frei, Beweis: Prod-Build ohne .env/.env.local grün (Task-4-Garantie hält); DrizzleAdapter mit explizit übergebenen Tabellen, Session-Strategie database, trustHost true, KEIN AUTH_URL (URLs aus Request-Host, Magic Links zeigen automatisch auf das jeweilige Deployment inkl. Previews). Resend-Provider mit eigener minimaler sendVerificationRequest (src/lib/email/send-verification-request.ts, deutscher Text-only, Du-Ansprache): fail-closed, jeder Fehler wirft; Sentry-Events NUR für eigene Fehler (401 Key, 5xx, Netzwerk, fehlende Provider-Config) — fremd-auslösbare Ablehnungen (403 Sandbox, 422, 429, übrige 4xx) werfen OHNE Sentry-Event, sonst leert ein Fremder über den öffentlichen Signin-Endpoint die Sentry-Quota. ABHÄNGIGKEIT Task 9: Bis das Ratelimit auf dem Magic-Link-Versand steht, ist diese Klassifizierung der einzige Quota-Schutz; beim Task-9-Merge prüfen, dass der Guard in sendVerificationRequest sitzt. serverEnv() um AUTH_SECRET/AUTH_RESEND_KEY/EMAIL_FROM als Pflichtfelder erweitert (Health-Route verlangt sie jetzt mit — in allen Vercel-Environments gesetzt). ESLint-ignores um src/auth.ts ergänzt (die in Task 6 angekündigte einzige Adapter-Ausnahme). Tests: tests/db/auth-schema.test.ts (PGlite mit echter Migration 0001: Roundtrips, Unique-Email, FK plus Cascade, Compound-PKs auf verification_tokens und accounts); messageChain-Helper nach tests/helpers/db.ts gezogen (vorher nur in schema.test.ts). Env-Befund lokal: vercel env pull schreibt nur AUTH_*/Sentry nach .env.local, DATABASE_URL/DATABASE_URL_UNPOOLED bleiben in .env aus dem Neon-Onboarding (Integration setzt sie nur in Production statisch) — Node-Skripte brauchen beide --env-file. Beweis erbracht (PR #4-Preview): Signin-Seite rendert, Magic-Link-Mail zugestellt, Klick erzeugt Session — auf dem Preview-DB-Branch (ep-red-glade ≠ Production) verifiziert: users-Row dominik@maierai.com mit email_verified, sessions-Row verknüpft (expires +30 Tage), accounts leer (Email-Provider erzeugt keine Rows, korrekt). Befunde: (1) Die Resend-Signup-Adresse ist dominik@maierai.com, NICHT dominik.maier049@gmail.com — erster Testlauf scheiterte mit Resend-403 (Sandbox erlaubt nur die Signup-Adresse); Abnahme-Checkliste in docs/phase-0-plan.md korrigiert. Diagnoseweg: Vercel-Runtime-Log zeigt den fail-closed-Throw mit Status, Sentry blieb leer, weil 403 als fremd-auslösbar klassifiziert ist — der Quota-Schutz hat sich im ersten Praxisfall bewährt. (2) Auth.js mappt jeden Nicht-AuthError aus sendVerificationRequest auf die generische englische Seite error=Configuration — erklärende deutsche Fehlerseite ist als verbindlicher 7b-Punkt im Phase-Plan verankert (Brand Book 4.3 Regel 4). (3) Fehlgeschlagene Sends hinterlassen verwaiste verification_tokens-Rows (Token wird parallel zum Versand geschrieben) — expiren nach 24 h, harmlos. Nächster Task: 7b (Mail-Template, Fehlerpfade, Dev-Log-Login).
- 26.07.2026: Task 7b fertig (direkt auf main): Deutsches HTML+Text-Mail-Template als pure Funktion (src/lib/email/magic-link-template.ts): Table-Layout bewusst statt Divs — Gmail könnte beides (caniemail geprüft), aber Outlook/Windows wendet max-width nur auf Tables an; border-radius degradiert dort nur zu eckigen Kanten. Brand-Farben aus Brand Book 5.2 als Hex-Konstanten plus System-Font-Stacks als dokumentierte E-Mail-Ausnahme (Clients können weder CSS-Variablen noch self-hosted Fonts); CTA-Button ist das eine Rubrikrot-Element; URL wird HTML-escaped (Query-&). Dev-Log-Login fail-closed per Code (src/lib/email/dev-log.ts): loggt den Magic Link NUR bei AUTH_EMAIL_DEV_LOG=1 UND NODE_ENV exakt development UND weder VERCEL noch VERCEL_ENV gesetzt (leerer String zählt als gesetzt); jede Abweichung → regulärer Versand, nie Log, kein Abbruch-Pfad; Beweismatrix in tests/email/dev-log.test.ts plus Versandebenen-Beweis (Flag+VERCEL → fetch läuft). Fehlerpfade: MagicLinkSendError extends AuthError mit static type "AccessDenied" — im @auth/core-0.41.3-Quelltext verifiziert: nur die fixe Client-Safe-Liste überlebt den Redirect als ?error=, alles andere kollabiert zu Configuration, und AccessDenied ist der einzige client-safe Typ mit passender Semantik und kind "error" (landet auf pages.error). Deutsche Fehlerseite src/app/anmelde-fehler (pages.error in src/auth.ts): AccessDenied → Mail nicht verschickt, Verification → Link abgelaufen/verbraucht (wirft Auth.js selbst), Rest generisch; Mapping als pure Funktion getestet. WICHTIG: Die AccessDenied-Eindeutigkeit gilt nur ohne signIn-Callback — authConfig ist jetzt exportiert und tests/auth/auth-config.test.ts schlägt mit Erklärtext fehl, sobald jemand einen signIn-Callback einführt (dann eigenen Fehlertyp + Mapping trennen). Befunde: (1) vitest braucht server.deps.inline für next-auth/@auth/core (deren ESM importiert next/server ohne Extension, Node-Resolver lehnt ab, Vite löst es inline auf); (2) die Resend-Provider-Factory legt Optionen nur unter options ab, das Merging macht Auth.js erst zur Laufzeit — Tests müssen den gemergten Provider nachbilden ({ ...Resend({}), apiKey, from }). Verify-Request-Seite bewusst nicht angefasst (kommt mit Login-UI in Task 8). Nächster Task: 8 (Login-UI und Routen-Schutz).
- 26.07.2026: Task 8 fertig (PR-Flow): Login-UI und Routen-Schutz. Routen: `/login` unter `src/app/(auth)` (Server Action mit Zod, `signIn("resend", { redirect: false })`, Erfolgs- UND Sendefehler-Zustand inline im Formular), `/login/verschickt` als statisches `pages.verifyRequest`-Ziel (deckt Nicht-Formular-Flows ab, sonst englische Auth.js-Default-Seite), `/start` als bewusst leere App-Route unter `src/app/(app)` (dauerhafter Einstiegspunkt nach Login, PRD-Entscheidungs-Log Nr. 7; Inhalt kommt mit 10b). Schutz in drei Ebenen: `src/proxy.ts` (Next 16: export function proxy; NUR optimistischer Cookie-Existenz-Check auf `[__Secure-]authjs.session-token`, setzt callbackUrl, Matcher `["/start"]` — 10b MUSS ihn um seine Routen erweitern), `(app)/layout.tsx` als echte Grenze (auth() gegen DB-Session, fängt verwaiste/widerrufene Cookies, Redirect bewusst OHNE callbackUrl — kein client-beeinflussbarer Header als zweite Quelle), Login-Page bounced bestehende Sessions (echte Session-Prüfung, kein Loop mit dem Proxy möglich). `sanitizeCallbackPath` in src/lib/auth/login-validation.ts als unit-getesteter Open-Redirect-Schutz (nur relative Pfade, kein //, kein Backslash, /api und /login gesperrt). Befund am @auth/core-0.41.3-Quelltext: mit `redirect: false` läuft signIn im raw-Modus und AuthErrors (MagicLinkSendError → AccessDenied) WERFEN in die Action statt zu redirecten — Sendefehler nutzen deshalb inline dieselbe `authErrorContent`-Mapping-Funktion aus 7b (kein Text doppelt); `/anmelde-fehler` bleibt für Auth.js-eigene Redirects (abgelaufener Link). NEXT_REDIRECT-Regel: try/catch nur um den nicht-redirectenden signIn-Call, signOut nie wrappen. Scaffold-Root-Page gelöscht: `/` ist bewusst unbelegt (deutsche `src/app/not-found.tsx` mit Wortmarke und Login-Link) — die Root-Zuordnung ist OFFEN und wird beim Landingpage-Einschub entschieden; `(app)` und `(marketing)` dürfen nie gleichzeitig eine page.tsx auf `/` haben, abgesichert durch tests/app/route-conflicts.test.ts (scannt src/app rekursiv, rechnet Route Groups raus, max. eine page-Datei pro URL — Ersatz für den seit Turbopack 16.2.11 fehlenden Build-Fehler, Task-2-Befund). Global eine deutsche `src/app/error.tsx` (Client, Sentry.captureException, Handlungsoption). Erste Brand-Bausteine: `src/components/brand/wordmark.tsx` (Besley-Wortmarke plus Freigabe-Haken als eine SVG-Kurve in Rubrikrot) und `(auth)/auth-card.tsx` (Korrekturabzug-Karte mit Beschnittmarken in --line). Pflicht-Checks Kap. 7 dokumentiert: Tausch-Test bestanden (Haken, Beschnittmarken, Mono-Maschinenzeile nur im Erfolgszustand), Ein-Rot-Regel (einziges Rubrikrot: der Haken; Validierungsfehler in status-error als funktionale Statusfarbe), Kontrast maschinell (alle Textpaare ≥ 6,59:1; Befund: Input-Border --line auf Weiß 1,38:1 unter der 3:1-Nicht-Text-Marke — Token-Mapping aus Task 1a, mitigiert durch Label und Ink-Fokusring, Kandidat für spätere Schärfung), Motion-Gate maschinell (prefers-reduced-motion → animation: none; einzige Animation ist der 150ms-Fade des Formularwechsels), Chanel-Regel (Kartenschatten entfernt, Bogen liegt flach). E2E lokal via Dev-Log-Login bewiesen (404 auf /, Proxy-Redirect mit callbackUrl, Zod-Fehler deutsch, Erfolgsanzeige mit Adresse, Magic-Link-Klick bis /start, Login-Bounce, Abmelden löscht Session, verwaistes Cookie → Layout fängt); Test-User danach aus der Prod-DB entfernt. WICHTIG/OFFEN: Mit diesem Task existiert erstmals ein öffentliches, mail-auslösendes Formular ohne Ratelimit — bis Task 9 gemergt ist, ist die Fehlerklassifizierung aus 7a der einzige Quota-Schutz. Task 9 folgt unmittelbar, nichts dazwischen.
- 26.07.2026: Nacharbeit nach Task 8 (ein Commit direkt auf main, CI und Production nach Task-8-Merge verifiziert: main-CI grün, /api/health mit Merge-SHA und db ok, / liefert deutsche 404, /start leitet um): (1) Neue Niemals-Regel: keine schreibenden Zugriffe auf die Produktions-DB, auch nicht durch lokale Test- oder Browser-Läufe, auch nicht mit anschließendem Aufräumen (Anlass: der Task-8-E2E-Lauf schrieb per Dev-Log-Login in die Prod-DB und räumte danach auf — bleibt trotzdem ein Verstoß); Konvention ergänzt: lokale Entwicklung gegen eigenen Neon-Dev-Branch, Prod-DATABASE_URL nur als bewusster Betreiber-Akt für Abnahme-Schritte 5 und 7. (2) Task 10b im Phase-Plan an die Task-8-Realität angepasst: kein `(app)/page.tsx`, `/` bleibt bis zum Landingpage-Einschub unbelegt, Workspace-Liste lebt dauerhaft auf `/start` (semantisch neutraler Einstiegspunkt: heute Liste, ab Phase 1 voraussichtlich Redirect in den letzten Workspace — PRD-Entscheidungs-Log Nr. 7; Root-Offenheit als Nr. 6), route-conflicts-Test als Wächter benannt, Proxy-Matcher-Erweiterung (/onboarding, /w/:path*) expliziter 10b-Punkt; das Wort „temporär" ist aus allen /start-Stellen in Code und Docs entfernt. (3) Kontrast-Befund erledigt statt vertagt: neues Token `--line-control` #84837B für Bedienelement-Grenzen (maschinell: 3,52:1 auf --paper, 3,81:1 auf --paper-raised; Zielmarke mindestens 3,5:1 gegen den schlechteren Untergrund in Brand Book 5.2 dokumentiert — Felder können ab dem Brand-Profil-Editor direkt auf Papier sitzen), shadcn-Semantik `--input` auf das Token gemappt, `--line` bleibt dekorativ-strukturell ohne Kontrastanspruch, Fokusring unberührt. Prüfpunkt-Befund: Im installierten Preset hängt im Light-Mode NUR Input an `--input`; der Outline-Button (Abmelden im Header) nutzt border-input ausschließlich unter `dark:` und Phase 0 hat keinen Dark-Mode — Header pixelidentisch, Login-Karte mit dunklerem Feldrand re-geprüft (Ein-Rot, Tausch-Test, Chanel unverändert bestanden). Bekannte Nuance ohne heutiges Vorkommen: `disabled:bg-input/50` tönt Disabled-Felder künftig kräftiger, bei Bedarf Ein-Klassen-Fix. Nächster Task: 9 (Upstash Ratelimit).
- 26.07.2026: Task 9 fertig (PR-Flow): Upstash Ratelimit auf dem Magic-Link-Versand (@upstash/ratelimit 2.0.8 + @upstash/redis 1.38.0, exakt gepinnt). Damit ist die offene 7a/8-Abhängigkeit geschlossen: die Fehlerklassifizierung ist nicht mehr der einzige Quota-Schutz. Guard in `sendVerificationRequest` NACH dem Dev-Log-Kurzschluss (nachweislich lokal, mail-frei) und VOR allem Richtung Resend — deckt Formular UND direkte POSTs ab; am @auth/core-0.41.3-Quelltext verifiziert, dass `request` via `toRequest` an sendVerificationRequest durchgereicht wird. `src/lib/security/ratelimit.ts`: zwei Dimensionen SEQUENTIELL, E-Mail zuerst (slidingWindow 3 pro 15 min), bei Treffer sofort raus OHNE IP-Inkrement (sonst verbrennt ein ungeduldiger legitimer Nutzer nach dem E-Mail-Limit sein IP-Budget und ist eine Stunde gesperrt, obwohl das 15-min-Fenster längst offen wäre; per Test belegt: E-Mail-Treffer → IP-Limiter nicht aufgerufen), dann IP (slidingWindow 10 pro 1 h, gegen Adress-Rotation). IP ausschließlich aus `x-real-ip` (setzt Vercel selbst); bewusst KEIN x-forwarded-for-Fallback: Proxies hängen an statt zu ersetzen, ein selbst mitgeschickter Header stünde vorne und die Dimension limitierte Angreifer-gewählte Strings. Ohne IP (lokal) wird nur die E-Mail-Dimension geprüft. Keys: HMAC-SHA256 mit AUTH_SECRET (unsalted Hashes sind per Kandidatenliste rückprüfbar, keine Pseudonymisierung) über `normalizeLoginEmail` (aus login-validation extrahiert, EINE Normalisierung für Formular-Schema und Hashing) bzw. die IP — kein Klartext-PII bei Upstash; Prefix `deskwire:${VERCEL_ENV||"dev"}:magic-link:{email|ip}` trennt die Zählerräume in der einen Free-DB. Fail-open als dokumentierter, einziger begründeter Bruch mit fail-closed (Defense-in-depth, kein Daten-Gate; englischer Kommentar-Header): fehlende Env → Limiter deaktiviert + EINE Sentry-Warnung pro Prozess; `limit()`-Laufzeitfehler → Sentry-Event (unser Ausfall) + Versand läuft; Limit-TREFFER → KEIN Sentry-Event (fremd-auslösbar, 7a-Klassifizierung geprüft und getestet). BEFUND: `Redis.fromEnv()` wirft in @upstash/redis 1.38.0 NICHT mehr synchron bei fehlender Env (nur console.warn plus Client, der erst beim Request stirbt — hätte die Eine-Warnung-Semantik in ein Sentry-Event pro Request verwandelt); Stolperstein 10 ist damit veraltet, die Env-Präsenz wird explizit geprüft und Redis direkt konstruiert, try/catch bleibt als zweite Sicherung. `MagicLinkRateLimitError` als EIGENE Klasse neben MagicLinkSendError (ein Limit ist kein Sendefehler, andere Handlungsoption), static type weiterhin "AccessDenied" (7b: einziger passender client-safe Typ); am Quelltext verifiziert: raw-Modus rethrowt die ORIGINAL-Instanz (@auth/core index.js `if (isAuthError && isRaw && !isRedirect) throw error`) und der next-auth-Action-Wrapper hat kein eigenes try/catch → instanceof-Unterscheidung in der Server Action trägt. Neuer Formular-Zustand `rate_limited`: „Zu viele Anmeldeversuche. Bitte warte ein paar Minuten." inline im Login-Formular; Redirect-Pfad (direkte POSTs) degradiert akzeptiert zur generischen AccessDenied-Seite (mehr als der statische Typ überlebt den Redirect nicht, praktisch nur der Missbrauchs-Pfad). WICHTIG, kein DB-Schutz: Auth.js schreibt die verification_tokens-Row PARALLEL zum Versand (send-token.js: `Promise.all([sendRequest, createToken])`, 7a-Befund 3 erneut verifiziert) — das Limit verhindert die Mail, nicht den DB-Write; geblockte Versuche hinterlassen weiter Rows (24 h TTL, bounded, harmlos). Die naheliegende „Reparatur" wäre ein signIn-Callback, und genau davor warnt tests/auth/auth-config.test.ts (AccessDenied-Eindeutigkeit) — bewusst nicht angefasst. Tests offline mit Fake-Limitern (tests/security/ratelimit.test.ts: Hashing ohne Klartext, Sequenz, fail-open-Matrix inkl. Secret-Ausfall; send-verification-request um Guard-Fälle erweitert), CI weiter secret-frei. Nächster Task: 10a (Workspaces und Memberships).
- 27.07.2026: Task 10a fertig (direkt auf main): Workspaces und Memberships als getestete Logik, bewusst ohne UI und Routen (10b). Schema plus Migration 0002 (rein additiv): pgEnum `membership_role` 'owner'|'member' — nur eine Spalte, Rechte-Logik ist Phase 4 (Entscheidung 21); `memberships` mit Composite-PK (user_id, workspace_id) statt Surrogat-id (liefert das geforderte unique mit, Muster wie accounts aus 7a), beidseitige Cascade-FKs, Index auf workspace_id. WICHTIG, Migrations-Drift: 0002 ist AUSSCHLIESSLICH auf dem Neon-Dev-Branch angewendet (ep-silent-credit, read-only verifiziert: Tabelle, Enum-Werte, 3 Journal-Einträge) — Production ist ab hier EINE Migration hinterher; das Nachziehen ist ein bewusster Betreiber-Akt, geplant nach 10b. Helper: `src/db/workspaces.ts` `createWorkspaceWithOwner` (Workspace + Owner-Membership in EINER db.transaction, kein db.batch — PGlite-Parität) und `src/db/memberships.ts` `listWorkspacesForUser` (Join mit Rolle, älteste zuerst) / `findMembership` (→ Row oder null) — bewusst unscoped, weil tenancy-etablierend, gekapselt in src/db/** (Entscheidung 23), gegen generisches `DbClient` typisiert (Task-5-Befund). Gebundene App-Einstiege in src/db/index.ts mit EIGENEN Namen: `createWorkspaceAsOwner`, `getWorkspacesForUser`, `getMembership` (Muster scopedDb/getScopedDb; gleichnamige Exporte mit fast identischer Signatur wären ein Importfehler, den typecheck nicht fängt). ESLint-Regel aus Task 6 unverändert, keine neue Ausnahme. `src/lib/workspace.ts`: `requireWorkspaceMembership(userId, workspaceId, deps)` → Membership oder null (Nicht-Mitglied → null, nie false, nie throw — 404-Mapping ist 10b; ungültige workspaceId → null OHNE DB-Roundtrip, sonst würde ein roher URL-String in 10b als Postgres-22P02 werfen), `createWorkspaceForUser({userId, rawName}, deps)` als dependency-injizierte Kernlogik der Server Action (Zod: Name getrimmt 1–80, deutsche Meldung), deps-Default gebunden an @/db, im Test Fakes. Action `src/app/(app)/onboarding/actions.ts` als dünne Hülle (auth() plus formData; Session-Prüfung in der Action, weil Actions öffentliche POST-Endpoints sind) — keine page.tsx, also keine Route, route-conflicts-Test grün; erster Aufrufer kommt mit 10b. BEFUND Session-Callback (Auth-Code, vom Betreiber freigegeben): @auth/core 0.41.3 strippt AUCH bei Database-Strategie den Session-User auf name/email/image (Default-callbacks.session in lib/init.js:20) — auth() liefert ohne eigenen session-Callback KEINE User-Id. src/auth.ts hat jetzt `callbacks.session` (kopiert user.id in die Session) plus Module-Augmentation `Session.user.id` als Pflichtfeld (ohne sie wäre id string|undefined und createWorkspaceForUser trüge einen Zufalls-Pfad); tests/auth/auth-config.test.ts um zweiten Wächter erweitert (Callback-Präsenz UND Verhalten mit Erklärtext; Negativprobe belegt: Callback entfernt → Test rot). Der signIn-Callback-Wächter aus 7b bleibt unberührt und grün. Typ-Befund dabei: der session-Callback-Parametertyp intersected AdapterSession (expires: Date) mit Session (expires: ISO-String) — in sich widersprüchlich, zur Laufzeit kommt ein Date; im Test EINE gezielte Assertion `Date & string` mit Kommentar, kein as unknown as. Tests: tests/db/workspaces-memberships.test.ts (PGlite mit echter Migration 0002: Atomizitätsbeweis über FK-Fehler in der Transaktion → Workspace-Row nachweislich weg, Composite-PK-Duplikat rot, Cascades von Workspace- UND User-Seite, Liste sortiert und fremdfrei, findMembership-Fälle) und tests/lib/workspace.test.ts (Fake-Deps: ungültige UUID und ungültiger Name rufen die Dep nie auf, DB-Fehler werfen in zentrales Logging durch). Nächster Task: 10b (Onboarding, Workspace-Liste, Workspace-Routing).
- 27.07.2026: Task 10b fertig (PR-Flow): Onboarding, Workspace-Liste, Workspace-Routing. Routen: `/start` trägt die Workspace-Liste als minimalen Umschalter (Zeilenwerk mit `--line`-Trennern, Rolle als Beitext, „Workspace anlegen" als Outline-Link), `/onboarding` das Anlage-Formular (ProofSheet, schließt die 10a-Action an), `/w/[workspaceId]` mit `layout.tsx` als Autorisierungsgrenze: `auth()` + `requireWorkspaceMembership`, Nicht-Mitglied/unbekannt/ungültig → `notFound()` (404 statt 403, Entscheidungen 18/19; verifiziert: die DEUTSCHE not-found aus Task 8 rendert, eingeloggt im (app)-Header-Rahmen, HTTP 404, ungültige IDs ohne DB-Roundtrip/22P02). Die Page verlässt sich NICHT aufs Layout (parallel rendering), sondern lädt membership-first selbst (`getWorkspacesForUser` + find → notFound). ABWEICHUNG vom 10b-Wortlaut (Betreiber-Entscheidung): /onboarding bounct NICHT mehr bei vorhandener Membership — es dient auch dem Zweit-Workspace via /start-Link, sonst wäre Abnahme-Schritt 4 (zweiter Workspace im Umschalter) unerfüllbar. Schleifenfreiheit strukturell: `postLoginSurface(count)` in src/lib/workspace.ts ist die EINE Wahrheit (0 → onboarding, sonst start), nur /start redirectet (bei 0 nach /onboarding), /onboarding redirectet nirgendwohin — unit-getestet mit Erklärkommentar; wer /onboarding je wieder einen Redirect gibt, MUSS über dieselbe Funktion branchen. Action-Anschluss: `createWorkspaceAction` redirectet nach Anlage per `redirect(/w/{id})` (NEXT_REDIRECT, außerhalb jedes try/catch), `created`-FormState entfällt. BEFUND „use server": ein use-server-Modul darf NUR async Functions exportieren — Turbopack erzwingt das erst, sobald eine Client-Komponente das Modul importiert (10a-Konstante `WORKSPACE_LOGIN_REQUIRED_MESSAGE` ist jetzt modul-lokal; `export type` bleibt erlaubt, Beweis login/actions seit Task 8). Proxy-Matcher um `/onboarding` und `/w/:path*` erweitert (bleibt Komfort, Entscheidung 20). VERTAGT, bewusst: bis zu drei Session-DB-Lookups pro Request auf /w/* ((app)-Layout, w-Layout, Page, alle parallel) — geplanter Weg ist ein `React.cache`-Wrapper um `auth()` (neuer Helper, src/auth.ts unangetastet), erst bei echtem Bedarf. Umzug vorab als eigener Commit (Git-Rename, kein Markup-Diff): `(auth)/auth-card.tsx` → `src/components/brand/proof-sheet.tsx` (Export ProofSheet, Markenmotiv statt Auth-Detail). Brand-Pflicht-Checks Kap. 7 (alle drei Ansichten): Tausch-Test bestanden (Besley-Zeilenwerk, Korrekturabzug-Karte, kein Card-Grid-SaaS-Look), Ein-Rot maschinell (einziges Rot pro Viewport = Wortmarken-Haken; Fehlerzustand nutzt status-error als funktionale Statusfarbe, Task-8-Präzedenz), Kontrast maschinell (schlechtestes Textpaar ink-soft/paper 7,01:1, alle ≥ 4,5:1; line-control 3,52:1/3,81:1 ≥ 3,5), Motion-Gate (null Animationen, einzige Transition ist der 150ms-Farbwechsel der Zeilen-Hover, keine Bewegung), Chanel (der geplante Richtungspfeil in den Listenzeilen wurde gestrichen). E2E lokal via Playwright + Dev-Log-Login AUSSCHLIESSLICH gegen den Neon-Dev-Branch (ep-silent-credit, vor dem Lauf an der echten .env verifiziert; Test-User task10b-e2e@example.com mit zwei Workspaces verbleibt dort): kompletter Pfad Login → /onboarding → Anlage → /w/{id} → Liste → Zweit-Anlage ohne Bounce → Umschalter zeigt beide → fremde UUID 404 → ungültige ID 404 → Abmelden → callbackUrl-Roundtrip zurück in den Workspace. Preview-Beweis: PR-Checkliste. Nächster Task: 11 (Einladungslink).
- 27.07.2026: Task 11 fertig (PR-Flow): Einladungslink (Entscheidung 22: genau ein regenerierbarer Mehrfach-Link pro Workspace, 7 Tage, nicht e-mail-gebunden). Schema plus Migration 0003 (rein additiv): `workspace_invites` mit workspace_id als PK (erzwingt die Ein-Link-Regel strukturell, Erneuern ist ein Upsert auf den PK), token text unique (der Unique-Constraint IST der Einlöse-Index), created_by nullable mit ON DELETE SET NULL (der Link gehört dem Workspace, nicht dem Ersteller; cascade würde ihn mitreißen, restrict die User-Löschung blockieren). Token: `randomBytes(32).toString("base64url")` → 43 Zeichen, 256 Bit, bewusst im KLARTEXT gespeichert: die Settings-Seite muss den Link jederzeit wieder anzeigen („Link kopieren"), ein Hash könnte ihn nur einmalig zeigen; Risiko begrenzt durch Entropie, TTL, Regenerierbarkeit und den kleinen Grant (member-Beitritt) — CLAUDE.md-Prinzip 5 zielt auf externe Credentials, nicht first-party Bearer-Links. Schichten: scopedDb.invites.get/regenerate (Erzeugen+Erneuern als EIN race-sicherer `onConflictDoUpdate`-Upsert; updatedAt explizit gesetzt, `$onUpdate` feuert dort nicht) in src/db/scoped.ts; unscoped gekapselt src/db/invites.ts (`findValidInviteByToken(db, token, now)` — unscoped, weil der Einlösende noch kein Mitglied ist und die workspace_id bis zum Lookup unbekannt; `now` als Parameter macht Ablauf ohne Clock-Mocking testbar; abgelaufen und unbekannt bewusst ununterscheidbar, ein null, kein Gültigkeits-Orakel) und memberships.`createMembershipFromInvite` (Rolle hart 'member', `onConflictDoNothing` auf den Composite-PK liefert Idempotenz UND Rollenerhalt in einem Mechanismus: Owner löst eigenen Link ein → Konflikt → Row unberührt → bleibt Owner); gebundene Einstiege `getValidInvite`/`joinWorkspaceAsMember` (10a-Namensmuster), ESLint-Regel aus Task 6 unverändert. Flow src/lib/invite.ts: `parseInviteToken` (Zod, exakt 43 base64url-Zeichen, ungültige Form ohne DB-Roundtrip — 10b-Muster), `redeemInvite` mit injizierbaren Deps — die workspaceId des Beitritts stammt AUSSCHLIESSLICH aus der Invite-Row, nie aus Input; `isInviteExpired` lebt in der Lib, weil react-hooks/purity `Date.now()` im Render verbietet. Route /invite/[token] AUSSERHALB (app) (dessen Layout redirectet ohne callbackUrl, Task-8-Entscheidung), robots noindex, Login-zuerst wie im Phase-Plan: Anonyme können Token-Gültigkeit nicht abklopfen; Proxy-Matcher um /invite/:path* erweitert (setzt die callbackUrl), `sanitizeCallbackPath` lässt /invite/<token> nachweislich durch (neuer Testfall). Settings-Seite /w/[workspaceId]/settings (membership-first wie die Workspace-Page, parallel rendering): drei Zustände (kein Link / gültig mit Mono-URL aus Request-Host, Gültig-bis Europe/Berlin, „Link kopieren" mit Clipboard-Feedback „Kopiert." in Mono / abgelaufen), EIN Upsert hinter „Link erzeugen" und „Link erneuern". WICHTIG, bewusste Entscheidung: JEDES Mitglied darf erzeugen und erneuern — eine Owner-Beschränkung wäre Rechte-Logik und ist bewusst auf Phase 4 vertagt (Entscheidung 21); die Nebenwirkung steht als Hinweis auf der Seite („Beim Erneuern wird der bisherige Link ungültig, auch wenn ihn jemand anderes erzeugt hat.", Betreiber-Vorgabe). Tests: tests/db/invites.test.ts (PGlite migriert 0003 real mit: Token-Shape und 7-Tage-Fenster, Upsert ersetzt statt anzuhäufen, Token-Unique über Workspaces, abgelaufen/unbekannt → null, Idempotenz, Owner-bleibt-Owner, Cross-Tenant explizit: A-Token liefert nur A, erzeugt nichts in B, Bs Row bleibt bei As Erneuern unberührt), tests/lib/invite.test.ts (Fake-Deps: Shape-invalid ruft Deps nie, workspaceId aus der Row, Fehler werfen durch; formatInviteExpiry mit TZ-Beweis), Sanitizer-Fall in login-validation.test.ts. E2E lokal (Playwright + Dev-Log-Login, ausschließlich Dev-Branch ep-silent-credit, vorher an der .env verifiziert): Erzeugen → Kopieren (Clipboard-Inhalt exakt die URL) → Owner öffnet eigenen Link → bleibt Inhaber → Erneuern entwertet den alten Link → ohne Session leitet der Proxy mit callbackUrl → frischer User B via Magic Link → landet als „Mitglied" im RICHTIGEN Workspace → Link erneut öffnen idempotent → fremder Workspace für B 404 → Mitglied B sieht Settings mit beiden Aktionen; Fehlerseite für unbekannte UND shape-kaputte Tokens deutsch mit Handlungsoption. Test-User task11-invite@example.com verbleibt auf dem Dev-Branch. MIGRATIONS-STAND: 0003 ist NUR auf dem Dev-Branch angewendet (read-only verifiziert: Spalten, beide Indizes, 4 Journal-Einträge); der Production-Nachzug ist ein Betreiber-Akt und MUSS vor dem Preview-Beweis passieren (Preview-DB-Branches forken von Production, Task-4-Befund). Nächster Task: 12 (Tenancy-Isolationstest-Suite; workspace_invites gehört dort in die deklarative Entitätsliste).
- 27.07.2026: Task 12 fertig (direkt auf main): Tenancy-Isolationstest-Suite plus Schema-Meta-Test, reine Testarbeit (keine Migration, keine UI, null App-Code-Änderungen). `tests/helpers/tenancy.ts`: `seedTwoTenants(db)` — REGEL im Kommentar-Header: bleibt minimal (zwei User, zwei Workspaces mit Owner-Membership, zwei Scopes, NICHTS vorbefüllt; Fixtures im Seed wären Kopplung zwischen Suiten), unique E-Mails erlauben Mehrfach-Seeds pro PGlite-Instanz; dazu `SCOPE_EXEMPT_TABLES` (EINE Quelle für beide Tenancy-Tests statt Drift — bewusste Abweichung vom Plan-Wortlaut „im Testfile", dokumentiert: users/accounts/sessions/verification_tokens als Auth-Infrastruktur, workspaces als Tenant selbst, memberships/workspace_invites als tenancy-etablierend gemäß Entscheidung 23) und `schemaTables()` (programmatische Enumeration via `is(value, PgTable)` über die Schema-Exports; Typ-Befund: Predicate braucht `unknown[]`-Zwischenstufe, die konkreten Export-Typen sind enger als PgTable). `tests/tenancy/schema-scope.test.ts` (Entscheidung 24): jede nicht-exempte Tabelle MUSS `workspace_id` tragen UND NOT NULL (nullable Scope-Spalte = Rows außerhalb jedes Tenants), Verstoß → rot mit Handlungsanweisung; zweite Assertion gegen Verrottung: jeder Exempt-Eintrag muss im Schema existieren. `tests/tenancy/isolation.test.ts`: deklarative Entitätsliste (Eintrag = Tabelle + Form + Accessors/Methoden), Formen collection (generischer Runner: create-stempelt-Scope, list-fremd-leer, getById-fremd-null, getById-unbekannt-null; heute brandProfiles), singleton (invites: regenerate stempelt, get scope-blind, Erneuern lässt fremde Row unberührt) und custom (explizit benannte Fälle, Leer-Fallmenge verboten). DREI Vergessens-Wächter mit Erklärtext, alle per Negativprobe rot belegt: (1) Namespace-Abgleich `Object.keys(scopedDb())` minus workspaceId === Listen-Keys (neuer Namespace ohne Eintrag → rot), (2) Tabellen-Abdeckung: jede nicht-exempte Schema-Tabelle muss von einem Eintrag abgedeckt sein — Subset, nicht Gleichheit, weil workspace_invites exempt UND als Eintrag vorhanden ist (workspace_id allein beweist keine Isolation), (3) Form-Assertion: Namespace-Methoden EXAKT gleich deklarierter Form (Zusatzmethode am Collection-Namespace → rot; verhindert stilles Grün mit leerer Fallmenge — Betreiber-Ergänzung zum Plan). Dazu Memberships-Sicht (listWorkspacesForUser fremdfrei, findMembership fremd → null) und der Einlöse-Flow aus Task 11 (A-Token greift nur in A). Verschoben statt dupliziert: tests/db/brand-profiles.test.ts aufgelöst (alle fünf Fälle = Collection-Eintrag, Typ-Paritäts-Beweis trägt jetzt die Suite), aus invites.test.ts der tenant-isolation-Describe und der scope-blind-get-Fall (Mechanik bleibt dort), aus workspaces-memberships.test.ts der Nicht-Mitglied-Fall und die Fremdfrei-Assertion (Funktionales bleibt). Suite fand KEINEN Isolationsfehler im Bestand. 19 Testdateien, 170 Tests grün, CI weiter secret-frei. Task 16 erweitert die Liste um runs + credit_ledger (je ein Eintrag plus Factory). Nächster Task: 13 (brand_profiles-Stub-UI).
- 27.07.2026: Task 13 fertig (PR-Flow): brand_profiles-Stub-UI, Anlegen und Liste unter /w/[workspaceId]/brand-profiles — der sichtbare Testeintrag des Demo-Kriteriums, strikt Stub (Name plus Beschreibung; kein Edit, kein Delete, keine Versionierung, keine Detailseite; das Feldschema des echten Marken-Profils ist eine Phase-1-Entscheidung mit QA-Konsequenzen). MIGRATIONS-STAND geklärt (Betreiber-bestätigt): Production und Dev-Branch stehen BEIDE auf 0003 — 0002 wurde vor Task 10b, 0003 vor dem Task-11-Preview-Beweis als Betreiber-Akt nachgezogen und in der Neon-Console verifiziert; die „Production hinterher"-Passage aus dem Task-11-Eintrag ist damit erledigt und darf nicht erneut als Annahme dienen. Schichten: src/lib/brand-profile.ts mit DI-Kernlogik `createBrandProfileForMember({userId, workspaceId, rawName, rawDescription}, deps)` (Muster createWorkspaceForUser) — Reihenfolge bewusst: Zod zuerst (Name trim 1–80, Beschreibung optional trim max 500, leer/fehlend → null; Meldungen als Template-Literal-Konstanten), dann Membership-Gate über injizierbares requireWorkspaceMembership (null → forbidden mit Task-11-Wortlaut „Dafür fehlt dir die Berechtigung.", identisch für fremd und unbekannt, kein Existenz-Orakel; deckt via UUID-Gate auch kaputte IDs ohne DB-Roundtrip), dann Create über getScopedDb — die Kernlogik reicht NIE eine workspace_id in die Row, der Scope stempelt. Action als dünne Hülle (auth() in der Action, workspaceId aus Hidden-Input wie Task 11, Fehlerecho der Eingaben ins Formular; Erfolg = revalidatePath + idle, bewusst KEIN Success-Text und kein Redirect — der neue Eintrag in der Liste ist das Feedback). Seite membership-first selbst-schützend (10b-Muster, parallel rendering), Liste im /start-Zeilenmuster, Datum via formatBrandProfileDate (de-DE, Europe/Berlin gepinnt, Mono-Maschinenstimme), Leerzustand nach 4.3 („Noch keine Marken-Profile. Leg unten das erste an."); Zuwegung als Link auf der Workspace-Seite (Text dort auf „Marken-Profile" umgestellt, ein Begriff pro Ding). REGEL (Betreiber-bestätigt): Das ProofSheet-Motiv gehört den inszenierten Anlage-Momenten ((auth), /onboarding); Arbeitsseiten unter /w/ folgen dem Settings-Sektionsmuster (border-t-Sektionen, kein Karten-Motiv) — gilt für alle künftigen Arbeitsseiten, keine Einzelfallentscheidung mehr. Bundle-Befund: Die Client-Form importiert die Längen-Konstanten bewusst NICHT aus der Lib (deren Default-Deps zögen @/db und damit den Neon-Treiber ins Client-Bundle); maxLength steht hart als Progressive-Enhancement-Komfort, die Wahrheit ist die Zod-Grenze. Tests (tests/lib/brand-profile.test.ts, 15 neue, gesamt 185): Fake-Deps-Matrix (invalid ruft keine Dep, Beschreibungs-Normalisierung auf null inkl. fehlendem Feld, Grenzlängen 80/500, forbidden ohne create, DB-Fehler wirft durch) plus PGlite-Beweis über die ECHTE Kette (requireWorkspaceMembership mit PGlite-gebundenem findMembership plus scopedDb.create gegen die echten Migrationen, seedTwoTenants): Row trägt nachweislich die workspace_id des Mitglieds. Die Isolationsmatrix ist bewusst NICHT dupliziert — Task-12-Suite deckt sie und blieb unverändert grün (keine neuen scopedDb-Methoden, kein Schema-Change, alle drei Wächter still). Brand-Pflicht-Checks Kap. 7: Tausch-Test bestanden (Besley-Titel, Zeilenwerk, Mono-Datumszeile, kein Card-Grid), Ein-Rot (einziges Rot = Wortmarken-Haken; Validierungsfehler in status-error als funktionale Statusfarbe, Präzedenz 8/10b), Kontrast (null neue Farbpaare, alle Paare aus den 8/10b-Messungen), Motion (null neue Animationen und Transitions, Listenzeilen sind nicht interaktiv), Chanel mit echtem Fund: Doppellinie zwischen letzter Listenzeile (border-b) und Sektions-border-t — Liste auf divide-y umgestellt, genau eine Linie je Blockgrenze. E2E lokal (Playwright + Dev-Log-Login, ausschließlich Dev-Branch ep-silent-credit, vorher an der echten .env verifiziert; frischer User task13-e2e@example.com verbleibt dort mit Redaktion Alpha/Beta): Login → Onboarding → Marken-Profile leer → Whitespace-Name → deutsche Zod-Meldung inline mit Beschreibungs-Echo und aria-invalid → „Profil Alpha" mit Beschreibung → Zeile mit Datum, Formular-Reset → „Redaktion Beta": Liste leer (Disjunktheit Richtung 1) → „Profil Beta" ohne Beschreibung (keine Beschreibungszeile) → Alpha unverändert nur „Profil Alpha" (Richtung 2) → echter fremder Workspace (Task-11-„Redaktion Nord") HTTP 404 mit deutscher not-found im (app)-Rahmen → kaputte ID HTTP 404 ohne 22P02 im Server-Log → Abmelden → anonymer Aufruf → Proxy-Redirect mit callbackUrl → Magic Link mit callbackUrl landet direkt wieder auf der Marken-Profil-Seite. Preview-Beweis (zwei Workspaces, disjunkte Listen — das halbe Demo-Kriterium) als PR-Checkliste, Migrationen dafür komplett auf Production. Nächster Task: 14 (Stripe: Schema, Sync-Logik, Feature-Gates als Code).
- 27.07.2026: Task 14 fertig (direkt auf main): Stripe-Grundgerüst als reine Offline-Logik — Schema, Sync, Gate; kein Transportweg (15a), keine Checkout-UI, kein Badge (15b). Dep stripe 22.3.2 exakt gepinnt, KEIN apiVersion-Override (Entscheidung 25); am installierten Paket verifiziert: SDK-Pin `2026-06-24.dahlia` (cjs/apiVersion.js), und ohne Override trägt der Client trotzdem exakt diese Version (`props.apiVersion || DEFAULT_API_VERSION`, stripe.core.js) — der Kein-Override-Test prüft deshalb GLEICHHEIT mit dem Pin statt undefined und wird bei jedem SDK-Bump, der den Pin bewegt, absichtlich rot (Signal: Dashboard-Webhook-Endpoint-Version für 15b neu abgleichen). Basil-Falle an den v22-Typen belegt statt angenommen: `current_period_end` existiert auf Stripe.Subscription nicht als Feld (einziger Nicht-Kommentar-Treffer ist ein List-Filter-Param), liegt auf Stripe.SubscriptionItem — sync liest `items.data[0]` (Ein-Preis-Modell Phase 0, Zod min(1)). Schema plus Migration 0004 (rein additiv): workspaces +5 nullable Spalten (stripe_customer_id UNIQUE — der Constraint ist die strukturelle Basis der Genau-ein-Treffer-Annahme des Sync-Helpers, per PGlite-Test bewiesen: zweiter Workspace mit derselben Customer-Id stirbt am Constraint), `stripe_events` (event_id text pk, type, processed_at) als Idempotenz-Ledger (Entscheidung 27) — in 14 fasst KEIN Code die Tabelle an, insert-on-conflict kommt mit 15a. AUSLEGUNG (genehmigt) text statt pgEnum für subscription_status: Stripe dokumentiert Enum-Erweiterungen als non-breaking und liefert sie auch auf gepinnten API-Versionen (so kam `paused`) — ein pgEnum erzwänge pro neuem Status eine Migration; die Werteliste erzwingt die Zod-Grenze in sync.ts (Entscheidung 26), TS-seitig via `$type<StripeSubscriptionStatus>`. src/lib/billing/subscription-status.ts hält die 8 Statuswerte als EINE Quelle (Spalten-Typ, z.enum, Gate-Matrix) mit Compile-Tripwires in beide Richtungen (`satisfies` gegen die SDK-Union plus ExpectNever auf Exclude): ein SDK-Bump mit neuem Status macht typecheck rot — dann Liste erweitern und Gate-Matrix ergänzen. MIGRATIONS-STAND: 0004 ist NUR auf dem Dev-Branch angewendet (read-only verifiziert: Journal 5 Einträge, 0000–0003 mit unveränderten Timestamps, genau ein neuer; Spalten, stripe_events, beide Constraints vorhanden; Endpoint ep-silent-credit) — der Production-Nachzug ist ein bewusster Betreiber-Akt VOR dem 15b-Deploy. AUSLEGUNG (genehmigt), VERBINDLICH für 15b: STRIPE_SECRET_KEY ist in serverEnv() bewusst OPTIONAL (serverEnv ist alles-oder-nichts und wird von Health und Auth pro Request geparst; Production bekommt den Key erst vor dem 15b-Deploy, ein Pflichtfeld hätte Production am Task-14-Merge gebrochen); getStripe() wirft fail-closed mit klarer Meldung, leerer String ist Config-Fehler (min(1) wirft laut), nur ABWESENHEIT ist der gültige Noch-nicht-Zustand. Der 15b-Merge stellt das Feld auf Pflicht und prüft dasselbe für STRIPE_WEBHOOK_SECRET. Schichten: src/db/billing.ts unscoped-gekapselt (Entscheidung 23) mit `updateWorkspaceSubscriptionByStripeCustomerId` als EINEM atomaren UPDATE…WHERE stripe_customer_id RETURNING (kein Read-then-Write; unbekannter Customer = 0 Rows = null); bewusst KEIN gebundener Einstieg in src/db/index.ts — einziger Aufrufer ist sync.ts, db-injiziert, 15a reicht sein db durch. src/lib/billing/sync.ts: `syncStripeEvent(db, event: unknown)` — Event als unknown, weil constructEvent nur die Signatur beweist (Entscheidung 26); `HANDLED_STRIPE_EVENT_TYPES` exportiert, 15a MUSS gegen dieselbe Konstante filtern (keine Listen-Drift); Zod exakt auf die gelesenen Felder: id, customer als String (expandiertes Objekt = gewollter Parse-Fehler), status als z.enum, `items.data[0]` mit current_period_end und price.product; deleted → canceled HART im Code, nie aus dem Payload (Prinzip 1), die übrigen Felder bleiben als historische Spur gesynct. Fehlerpfade als typisiertes Ergebnis, nie Exception: Parse-Fehler → Sentry.captureException(ZodError) mit Event-Id/-Typ als Kontext (NIE der Payload) → skipped/invalid_payload; unbekannter Customer → Sentry-WARNUNG → skipped/unknown_customer (legitim erreichbar: Customer noch nicht via Drizzle Studio verknüpft, Entscheidung 28); beide Skips sind für die Zustellung final, 15a antwortet 200 (Begründung am Ergebnistyp: Retry macht strukturell Kaputtes nie gültig, Sentry ist der Alarm, nicht der Retry). Dokumentierte Nebenwirkung: ein unbekannter neuer Stripe-Status läuft in den Parse-Fehler-Pfad, der Workspace behält bis zur Listen-Erweiterung seinen letzten Status (das Sentry-Event ist das Signal). Dokumentierte Nicht-Behandlung: Out-of-order-Zustellung (spätes updated nach deleted gewinnt) — 15a dedupliziert nur; das Badge ist informativ, echte Verbrauchs-Gates kommen mit Phase 1. Gate src/lib/billing/gates.ts: `hasActiveSubscription(Pick<Workspace,"subscriptionStatus">)` = active ODER trialing, alles andere inkl. null fail-closed — das EINZIGE Phase-0-Gate. Tenancy: stripe_events in SCOPE_EXEMPT_TABLES (Transport-Infrastruktur, Begründung am Eintrag); Negativprobe belegt: ohne Eintrag beide Wächter rot (Schema-Meta plus Abdeckung) mit stripe_events im Fehlertext. Tests (28 neue, gesamt 213, CI weiter secret-frei): Event-Fixtures als TS-Literale mit `satisfies` gegen Stripe.CustomerSubscription{Created,Updated,Deleted}Event — BEWUSST keine .json-Dateien, JSON-Importe weiten Literaltypen ("active" → string) und könnten kein satisfies bestehen; so bewacht der SDK-Typ die Fixture-Form statt nur unser eigenes Zod-Schema. Tests schicken die Fixtures per JSON-Roundtrip als unknown durch sync (Serialisierbarkeits- plus Grenz-Beweis). Fixture-Abweichung dokumentiert: unit_amount_decimal/amount_decimal sind null, v22 typisiert sie als branded Decimal-Klasseninstanzen, die kein JSON-Literal erfüllen kann — Felder, die sync nie liest. RESTRISIKO: der Abgleich gegen echte Stripe-Payloads ist erst der 15b-Testmode-Durchlauf. Sync-Suite (PGlite, echte Migration 0004, seedTwoTenants): Lifecycle created/updated/deleted mit exakter Date-Konversion aus Unix-Sekunden, deleted mit lügendem Payload-Status → trotzdem canceled (Code entscheidet), Cross-Tenant byte-identisch, unknown_customer ohne Update, 7 invalid-Varianten (je Sentry-Event plus DB unberührt), stripe_events-PK-Beweis, Unique-Beweis; Gate-Status-Matrix mit Vollständigkeits-Wächter (Matrix MUSS jeden Statuswert plus null abdecken, sonst rot). Nächster Task: 15a (Stripe-Webhook-Route: Signatur und Idempotenz).
- 27.07.2026: Task 15a fertig (direkt auf main): Stripe-Webhook-Route mit Signaturprüfung und Idempotenz, vollständig offline getestet; kein Dashboard-Endpoint, kein CLI-Setup, kein Badge, keine Vercel-Env-Änderung (alles 15b), keine Migration. Zuschnitt: Kernfunktion `handleStripeWebhook(request, { db, stripe, webhookSecret })` in src/lib/billing/webhook.ts, komplett dep-injiziert; db-Zugang über gebundenen Einstieg `processStripeWebhook(request, transport)` in src/db/index.ts (Task-10a/11-Namensmuster, bindet NUR getDb — der Webhook ist der eine Route-Flow, der den Tenant AUS der Zustellung auflöst; die ESLint-Regel aus Task 6 bleibt mit exakt einer Ausnahme intakt, und der Barrel bleibt Stripe-SDK-frei, weil webhook.ts Stripe nur als Typ importiert); Route src/app/api/stripe/webhook/route.ts als dünner Wrapper. NODE-RUNTIME VERBINDLICH: kein `export const runtime = "edge"`, constructEvent braucht Node-Crypto — als Kommentar in der Route verankert, Build verifiziert die Route als ƒ Dynamic auf Node-Default. Ablauf fix: Secret fehlt → 500 vor jedem constructEvent (Check testbar in der Kernfunktion, fail-closed, nie ungeprüfte Verarbeitung); Header fehlt / Signatur falsch / Body manipuliert / Timestamp älter 300 s → 400; Typ-Filter ausschließlich gegen HANDLED_STRIPE_EVENT_TYPES aus sync.ts (keine zweite Liste) VOR dem Idempotenz-Insert; dann EINE db.transaction um insert-on-conflict-do-nothing auf stripe_events plus syncStripeEvent(tx) — Duplikat (leeres returning) → 200 ohne Sync; updated und beide Skips (invalid_payload, unknown_customer) → ununterscheidbar 200, Rows committen (Zustellung final, Task-14-Semantik, Sentry macht sync selbst, Route reportet nichts doppelt). SENTRY-KLASSIFIZIERUNG 400er, KONSEQUENZ FÜR 15B: Signaturfehler sind fremd-auslösbar (öffentliche URL) und erzeugen KEIN Sentry-Event (7a-Muster) — auch das falsche whsec_ aus Stolperstein 9 (CLI- vs. Dashboard-Secret) bleibt in Sentry unsichtbar, die Diagnose läuft über die Zustellungsansicht des Stripe-Dashboard-Endpoints. UNBEKANNTE EVENT-TYPEN werden NICHT in stripe_events registriert: die Tabelle bedeutet „diese Zustellung wurde verarbeitet" (Entscheidung 27), und wächst die Handled-Liste später, dürfen bereits zugestellte Events des neuen Typs nicht als Duplikat vorverbrannt sein. RETRY-INVARIANTE: Wirft der Sync unerwartet, rollt die Transaktion die stripe_events-Row mit zurück, der Fehler wirft durch Route und Next (500, onRequestError → Sentry, Task 3), Stripes Retry findet KEIN Duplikat vor und verarbeitet frisch — ein fehlgeschlagener Versuch kann ein Event nie dauerhaft als verarbeitet brennen. Getestet per Fault-Injection über die Daten (keine Mocks): current_period_end 1e14 Sekunden passiert Zod (positive int), ergibt aber ein Out-of-Range-JS-Date, und der Drizzle-Timestamp-Writer wirft RangeError mitten im Update (am Quelltext verifiziert: timestamp withTimezone serialisiert via value.toISOString()); Assertions: rejects, Row weg, identische event_id mit gesundem Payload danach → 200 plus Workspace-Update. WRAPPER-GUARD (bewusste Ergänzung): Die Route prüft STRIPE_WEBHOOK_SECRET, BEVOR sie getStripe() aufruft — getStripe wirft bei fehlendem STRIPE_SECRET_KEY (Prod-Zustand bis 15b), und dieser Throw würde jeden Scanner-POST via onRequestError in ein fremd-auslösbares Sentry-Event verwandeln; der testbare 500-Pfad in der Kernfunktion bleibt die Wahrheit, der Guard ist reine Quota-Hygiene. serverEnv: STRIPE_WEBHOOK_SECRET optional exakt nach dem STRIPE_SECRET_KEY-Muster (leerer String wirft laut, nur Abwesenheit ist der gültige Noch-nicht-Zustand); die 15b-Pflicht-Wanderung gilt unverändert für BEIDE Felder. Proxy-Matcher-Analyse: kein Pattern beginnt mit /api, die Route ist anonym per POST erreichbar, einzige Autorisierung ist die Signatur; jeder Testfall läuft mit sessionlosem new Request(). Tests offline (tests/billing/webhook.test.ts, 11 neue, plus 2 Env-Fälle, gesamt 226, CI weiter secret-frei): Signieren mit echtem SDK (new Stripe mit Dummy-Key, generateTestHeaderString — reine Krypto, kein Netz), Fixtures aus Task 14 per JSON-Roundtrip-Klon wiederverwendet, PGlite mit echten Migrationen; Matrix: gültig → 200 plus vier Felder plus Tenant B byte-identisch, vier Signatur-Fälle → 400 plus DB unberührt plus kein Sentry, Duplikat → 200 ohne zweite Verarbeitung (manuell verstellter Status bleibt), unbekannter Typ → 200 ohne Row, beide Skips → 200 mit Row, Secret undefined → 500 plus constructEvent-Spy nachweislich nicht aufgerufen, Invariante wie oben. Prod-Build lokal grün. Nächster Task: 15b (Stripe-Testmode-Durchlauf und Status-Badge; vorher Betreiber-Akte: Migration 0004 auf Production, beide Secrets in Vercel, Dashboard-Endpoint auf Dahlia-Pin).
- 27.07.2026: Task 15b Code-Teil fertig (direkt auf main): deutsches Abo-Status-Badge plus README-Abschnitt „Stripe-Testmode-Setup" — strikt Anzeige und Doku, keine Billing-UI, kein Checkout, kein Portal, kein neuer Gate, keine Migration, keine Dependency, keine neue Env. Vorbedingungen waren erledigt (Betreiber-bestätigt): Migration 0004 auf Production, Produkt/Preis im Testmode, Test-Customer per Drizzle Studio mit „Redaktion Alpha" verknüpft, Dashboard-Endpoint auf Prod-URL mit den drei Subscription-Events und API-Version 2026-06-24.dahlia, beide Secrets in Vercel Production, Redeploy durch, ungesignter POST → 400 verifiziert. Badge-Prinzip: **das Badge ist das Gate in Sprache** — `hasActiveSubscription` (Task 14) ist die einzige Wahrheit für „hat ein Abo", jeder Gate-false-Status liest sich als „Abo: keins" (Betreiber-Entscheidung statt Plan-Wortlaut „kein Abo": Substantiv-Wiederholung im eigenen Wert, Maschinenstimme sagt es knapp), das Badge behauptet nie mehr als das Gate gewährt; aktiv/Testphase ist reine Anzeige-Verfeinerung im Gate-true-Ast, kein zweiter Statusbegriff. src/lib/billing/subscription-badge.ts: Label-Konstanten plus `BADGE_BY_STATUS` als `satisfies Record<StripeSubscriptionStatus, SubscriptionBadgeLabel>` — exhaustiv per Konstruktion, wächst die Task-14-Statusliste (SDK-Bump-Tripwire), wird der fehlende Schlüssel hier compile-rot; `subscriptionBadgeLabel(Pick<Workspace,"subscriptionStatus">)` spiegelt die Gate-Signatur, null explizit → keins, keine if-Kaskade, kein else-Fallback. Matrix: active → aktiv, trialing → Testphase, canceled/incomplete/incomplete_expired/past_due/paused/unpaid/null → keins (past_due/unpaid bewusst ohne eigenen Text: Handlungsaufforderung ohne Handlungsmöglichkeit, es gibt kein Portal; null und canceled bewusst ununterscheidbar, „nie gehabt vs. verloren" ist Billing-UI). KEIN Datum im Badge: current_period_end bleibt gesynct und unangezeigt (wäre der erste Satz einer Billing-UI, bei canceled mit Restlaufzeit sofort erklärungsbedürftig). Einbau: Mono-Maschinenzeile (font-mono text-xs text-ink-soft) im Kopfblock von /w/[workspaceId] direkt unter dem Namen — Task-11/13-Präzedenz (Status und Maschinenangaben sprechen Mono), bewusst KEIN Chip/Pill (Tausch-Test, neue Farbpaare); null neue Queries: `getWorkspacesForUser` liefert die volle Workspace-Row, subscriptionStatus liegt schon vor; keine Client-Komponente, kein scopedDb-Touch, Task-12-Suite unberührt. Tests (tests/billing/subscription-badge.test.ts, 11 neue, gesamt 237, CI weiter secret-frei, reine Pure-Function-Tests ohne PGlite): Vollständigkeits-Wächter (Matrix ≡ Statusliste plus null, Gates-Test-Muster), it.each über alle neun Fälle gegen den exakten deutschen Text, Gate-Konsistenz-Wächter iteriert über die Statusliste SELBST (nicht über die Matrix, keine Abhängigkeit von deren Vollständigkeit) und erzwingt `(Text ≠ keins) === hasActiveSubscription(...)` — wer je einen Gate-false-Status aktiv klingen lässt, sieht Rot mit Erklärtext. Brand-Pflicht-Checks Kap. 7: Tausch-Test bestanden (Mono-Zeile statt SaaS-Badge), Ein-Rot (alle drei Zustände ink-soft, kein neues Rot, auch nicht für „keins"), Kontrast (null neue Farbpaare, ink-soft/paper 7,01:1 aus Task 10b), Motion (null neue Animationen/Transitions), Chanel (kein Fund, Zeile rahmen- und linienlos, keine neue Sektionsgrenze). README „Stripe-Testmode-Setup" (nach „Deployment"): CLI ohne Homebrew (GitHub-Release-Binary bzw. Docker mit host.docker.internal), lokaler Flow `stripe listen --forward-to localhost:3000/api/stripe/webhook`, Zwei-Secrets-Falle explizit (CLI-whsec_ → .env.local, Dashboard-whsec_ → Vercel, verwechselt → stummer 400 OHNE Sentry-Event per 15a-Klassifizierung, Diagnose über die Dashboard-Zustellungsansicht), Endpoint-Konfiguration mit Dahlia-Pin plus direkt daneben der NUTZLAST-STIL-Hinweis (Betreiber-Ergänzung): „Momentaufnahme" (Snapshot), nicht „Thin" — Thin-Payloads tragen nur IDs statt des vollständigen Subscription-Objekts und jeder Sync liefe in den nur in Sentry sichtbaren invalid_payload-Skip; Customer-Verknüpfung per Drizzle Studio (Entscheidung 28), nur Platzhalter, keine Secrets. ENDE-ZU-ENDE-BEWEIS ERBRACHT (Betreiber-Akt nach dem Commit, Task 15b damit VOLLSTÄNDIG abgeschlossen): Testmode-Abo im Dashboard an „Redaktion Alpha (Test)" angelegt (aktiv, ohne Trial, Testkarte 4242) → Endpoint-Zustellung erfolgreich (1 Übermittlung, 0 fehlgeschlagen, Reaktionszeit ca. 3 s Cold Start) → Badge auf Alpha wechselte auf „Abo: aktiv", Beta blieb durchgehend „Abo: keins" → sofortige Kündigung → Badge zurück auf „Abo: keins", Zustellung erneut erfolgreich; keine Sentry-Events, keine Nacharbeit — das Task-15b-DoD (beide Richtungen, Workspace B unberührt) ist erfüllt. Nächster Task: 16 (Metering-Fundament: runs + credit_ledger; Isolationssuite um beide Entitäten erweitern).
- 27.07.2026: Task 16 fertig (direkt auf main): Metering-Fundament runs + credit_ledger als reine Schema-, Helper- und Testarbeit — kein Ausführer, keine Statusübergänge, keine UI, kein Credit-Verbrauch, kein run_steps, keine neue Dependency, keine neue Env. Schema plus Migration 0005 (rein additiv): `runs` (id, workspace_id FK cascade NOT NULL + Index, status run_status NOT NULL, started_at NOT NULL defaultNow, finished_at/tokens_in/tokens_out/cost_cents/error nullable, timestamps) und `credit_ledger` (id, workspace_id FK cascade NOT NULL + Index, delta integer NOT NULL, reason text NOT NULL, reference text nullable, created_at — bewusst OHNE updated_at: append-only heißt unveränderliche Rows, ein updated_at würde Update-Fähigkeit suggerieren, dokumentierte Abweichung vom PRD-Kap.-11-Pauschalsatz; kein credit_balance am Workspace, Saldo = SUM(delta), Entscheidung 30). `run_status` als pgEnum TROTZ text-Entscheidung für subscription_status (Task 14) — der Unterschied ist die Herkunft des Vokabulars: Stripe-Status sind extern und wachsen non-breaking auch auf gepinnten API-Versionen (ein pgEnum erzwänge fremdgetriebene Migrationen), Run-Status sind eigenes Vokabular und Statusübergänge sind Code; Erweiterung ist eine bewusste Entscheidung, deren korrekte Konsequenz die Migration ist (als Kommentar am Schema verankert). Ausdrücklich OHNE briefing_id/job_id/brand_profile_version_id: die Tabellen existieren nicht, die Spalten kommen als nullable FKs mit ihren Phasen (keine FKs auf nicht existierende Tabellen, Entscheidung 29). DREI BESTÄTIGTE ENTSCHEIDUNGEN (Betreiber-Go): (1) status NOT NULL ohne DB-Default, tokens/cost nullable statt Default 0 — ein Status-Default („neu = running") wäre die erste Lebenszyklus-Regel und Lebenszyklus ist Phase-1-Ausführer-Territorium; „nicht gemessen" muss von „0 gemessen" unterscheidbar bleiben, ein abgestürzter Run ohne Messung darf nicht wie ein kostenloser aussehen (fail-closed fürs Metering); started_at defaultNow ist reine Erzeugungs-Semantik, im Vokabular gibt es keinen Queued-Zustand, Row-Erzeugung = Start. (2) reason als text, keine engere Form: das Buchungsvokabular (Monatsgutschrift, Kauf, Verbrauch, Korrektur, …) gehört zur Phase-1+-Billing-Strecke, und auf reason brancht nie Kontrollfluss — der Saldo ist SUM(delta), egal warum gebucht wurde; status steuert Gates, reason dokumentiert für Menschen; Verengung später per `$type` ohne Migration; NOT NULL plus Laufzeit-Guard gegen Leerstring; reference nullable (eine Erstgutschrift referenziert nichts) und bewusst kein FK, sondern weicher Zeiger auf Künftiges (Run-Id, Invoice-Id). (3) bookCredits OHNE Saldo-Prüfung: in Phase 0 verbraucht nichts Credits (tote Regel ohne bewachten Flow), Unterdeckungs-Verhalten ist eine Verbrauchsstrecken-Entscheidung der Phase-1-Budget-Gates (laufen VOR dem LLM-Aufruf, nicht am Ledger-Write), und technisch würde aus dem Append ein Read-then-Write mit echtem Race (parallele Buchungen bestehen beide die Prüfung, es gibt keine Row zum Locken); der Ledger erzwingt Integrität, keine Geschäftsregeln — Negativ-Saldo ist erlaubt und getestet. CASCADE-VERMERK (Betreiber-Vorgabe): credit_ledger (wie runs) kaskadiert bei Workspace-Löschung — hier korrekt, weil der Workspace der Tenant ist und Tenant-Löschung keine scope-losen Rows zurücklassen darf (Prinzip 3); ERNEUT ZU PRÜFEN, sobald Abrechnung an echtem Geld hängt: ein Buchungsjournal wird normalerweise nicht mit seinem Gegenstand gelöscht. Kein Redesign jetzt, der Vermerk steht auch als Schema-Kommentar. Scoped Helper (src/db/scoped.ts; ESLint-Regel aus Task 6 unangetastet, Barrel nur um Typ-Re-Exports erweitert, kein neuer gebundener Einstieg): `runs` als collection exakt create/getById/list — bewusst KEIN update, einen Run abschließen ist ein Statusübergang (Phase 1), die Form-Assertion der Suite erzwingt bei jeder Erweiterung Deklarations- und Fall-Pflege; `creditLedger` bewusst KEINE collection, nur book/balance (getById/list wären erfundene Oberfläche ohne Phase-0-Aufrufer): book trägt die Integritäts-Guards am einzigen Schreibpfad (Number.isInteger und ≠ 0 fängt 2.5/NaN/Infinity als lauten Throw statt Treiberfehler; reason nicht leer), balance behandelt die Leer-Saldo-Falle explizit per `coalesce(sum(delta), 0)::int` — SUM über null Zeilen ist in Postgres NULL und int8-Summen kämen als String, Coalesce plus Cast garantiert die JS-Zahl 0 in beiden Treibern (int4-Überlauf erst jenseits ~2,1 Mrd. Credits, dann laut, nie still). src/lib/billing/credits.ts: bookCredits/getCreditBalance als dünne DI-Hüllen über den Scope (deps-Default getScopedDb, Muster 10a/13), kanonischer App-Einstieg und Landeplatz der Phase-1-Budget-Gates; bewusst ohne Membership-Prüfung (System-Primitiven für Pipeline/Jobs/Sync, das Berechtigungs-Gate gehört der aufrufenden Action). Task-12-Suite erweitert, KEIN neuer Exempt-Eintrag (beide Tabellen NOT NULL workspace_id, Schema-Meta-Test ohne Zutun grün): runs als collection-Eintrag (Factory create({status:"running"}), generischer Vier-Fälle-Runner), creditLedger als custom-Eintrag (methods [balance, book], drei benannte Cases, handgeschriebenes describe: book stempelt die Scope-Id; Leer-Saldo von B ist exakt die ZAHL 0, während A-Rows in der Tabelle liegen — schärfste Form der Falle; balance summiert nur eigene Buchungen, A +10−3=7 vs. B +100). Beide Negativproben erbracht: ohne creditLedger-Eintrag Namespace- UND Abdeckungs-Wächter rot mit credit_ledger im Fehlertext; Form-Assertion bei methods ["book"] rot mit exakter Anweisung (Namespace hat [balance, book], Deklaration erwartet [book]). Tests: tests/db/runs.test.ts (Roundtrip aller Token-/Kostenfelder mit exakten Timestamps, it.each über alle drei Enum-Werte, laufender Run mit Null-Feldern plus started_at-Default, error-Text, Workspace-Kaskade) und tests/billing/credits.test.ts (echte Kette ohne Fakes — die Funktionen haben keine eigene Verzweigung, deps tauscht nur den Neon- gegen den PGlite-Client: Roundtrip inkl. reference, Summe mit negativen Deltas → 40, Leer-Saldo strikt toBe(0), Guard-Matrix 0/2.5/NaN/Infinity plus Leer-/Whitespace-reason mit Beweis „4 Rows, Saldo 40 unverändert", Negativ-Saldo −5 erlaubt, Ledger-Kaskade). 263 Tests grün (26 neue), lint/typecheck grün, CI weiter secret-frei (PGlite migriert 0005 real mit). MIGRATIONS-STAND: 0005 ist NUR auf dem Dev-Branch angewendet — read-only verifiziert (Host ep-silent-credit-agxqooy5, __drizzle_migrations 6 Rows: die fünf alten mit unveränderten Timestamps identisch zu den Journal-when-Werten, genau ein neuer Eintrag 0005; beide Tabellen, alle drei Enum-Werte, Nullability exakt wie abgenommen, credit_ledger ohne updated_at-Spalte); der Production-Nachzug ist ein bewusster Betreiber-Akt VOR der Task-17-Abnahme (Checklistenpunkt 13 verlangt beide Tabellen in der Produktions-DB). Nächster Task: 17 (Phase-0-Abnahme und Doku).
- 27.07.2026: Task 17 fertig, PHASE 0 ABGESCHLOSSEN (direkt auf main, reiner Doku-Commit, null Code-Änderungen). ABNAHME BESTANDEN: Betreiber-Durchlauf auf Production am 27.07.2026, alle 14 Checklistenpunkte aus docs/phase-0-plan.md e) — Health ok inkl. DB-Ping (SHA 4eda054); Login-Kette komplett (Schutz-Redirect ohne Session, deutsche Magic-Link-Mail, Login); Redaktion Alpha und Beta mit „Profil Alpha" (mit Beschreibung) und „Profil Beta" (ohne); Einladungslink in Beta erzeugt, User B (abnahme-userb@example.com, lokal via Dev-Log-Login) als Mitglied in Beta aufgenommen, kaputter Token → deutsche Fehlerseite (nach Login, davor Proxy-Redirect mit callbackUrl wie gebaut); Isolation dreifach: UI (disjunkte Profil-Listen), URL-Manipulation als B auf die Alpha-Route → 404, SQL-Prüfung (jede brand_profiles-Zeile genau einem Workspace zugeordnet) plus CI grün und nachweislich ohne DATABASE_URL; Stripe-Testmode in beide Richtungen (Abo → Zustellung → Badge „Abo: aktiv" auf Alpha, Beta durchgehend „Abo: keins", sofortige Kündigung → zurück auf „keins"); Ratelimit nach drei Anforderungen deutsche Hinweis-Meldung statt Mail; Sentry-Beweis auf Production (DEBUG_SENTRY_ENABLED temporär, Event DESKWIRE-1 mit Source-Map-Stacktrace bis src/app/api/debug-sentry/route.ts:11 inkl. Suspect Commit, Variable danach entfernt, Route wieder 404); runs und credit_ledger auf Production, Migrationsstand 0000–0005 auf Production und Dev-Branch identisch (je 6 Journal-Einträge). DOKU-TEIL: README (Abschnitt „Lokales Setup" mit npm install → vercel env pull → db:migrate → dev, Dev-Branch-Konvention explizit, AUTH_EMAIL_DEV_LOG als Zweit-User-Weg, Health-Zeile aktualisiert), .env.example (STRIPE_WEBHOOK_SECRET ergänzt — die EINZIGE Variable, die der Code liest und die fehlte; Stripe-Kommentare an die Realität angepasst; Hinweis auf bewusst nicht gelistete Plattform-Variablen), CLAUDE.md (Phase-Zeile, Kommandos an die realen package.json-Scripts angeglichen, inngest:dev als „ab Phase 1" und test:e2e als „ab Phase 3" markiert statt behauptet), PRD-Entscheidungs-Log um vier Einträge (8: Single-Package statt Monorepo; 9: next-auth-v5-Beta-Pin, Better Auth als Wechseloption beobachten; 10: credit_ledger bereits in Phase 0; 11: Stripe-Secrets bleiben OPTIONAL in serverEnv — REVISION der Task-14/15a-Ankündigung „15b stellt auf Pflicht": beide Secrets liegen bewusst nur im Vercel-Production-Scope, keine Preview-Webhooks in Phase 0, Pflichtfelder würden jedes Preview-Deployment an der Env-Validierung brechen, Health 503 und Login tot; fail-closed sitzt in getStripe() und im Webhook-Route-Guard), Phase 0 in PRD Kap. 6 als erledigt markiert (mit Vermerk der bewussten Abweichungen OAuth vertagt und Single-Package), Abnahme-Checkliste in phase-0-plan.md abgehakt. OFFEN, eigener kleiner Commit VOR dem Tag: die Kommentare in src/lib/env.ts (und der Task-14/15a-Wortlaut oben) verweisen noch auf die überholte Pflicht-Wanderung („15b flips this to required") — sie müssen auf Entscheidung 11 umgeschrieben werden; env.ts ist Code und blieb im Doku-Commit bewusst unangetastet. Git-Tag v0.1.0-phase0 setzt der Betreiber selbst. Nächster Schritt: Phase-1-Planung (erster Punkt der Restschulden-Liste: env.ts-Kommentare, siehe OFFEN).
